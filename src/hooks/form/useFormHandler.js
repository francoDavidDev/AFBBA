// hooks/useFormHandler.js
export const useFormHandler = ({ form, setForm, setErrors, setLoading, setModalOpen }) => {
  const handleSubmit = async (e) => {
    // ... lógicas actuales
  };

  return { handleSubmit };
};
